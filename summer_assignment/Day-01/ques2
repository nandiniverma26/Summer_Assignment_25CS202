// wap to table
#include <stdio.h>

int main() {
    int n, i;

    printf("Enter an integer to generate its multiplication table: ");
    scanf("%d", &n);

    printf("\nMultiplication Table for %d:\n", n);
    printf("-----------------------------\n");

    for (i = 1; i <= 10; ++i) {
        printf("%d x %d = %d\n", n, i, n * i);
    }

    return 0;
}